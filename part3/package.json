{
  "name": "part3",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "cd FrontEnd && npm run build && rm -rf ../dist && cp -r dist ..",
    "deploy:full": "npm run build:ui && git add . && git commit -m 'update ui build' && git push"
  },
  "dependencies": {
    "cores": "^0.8.5",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}